uuid: 6c2ff2e4-8b52-41aa-9a6f-041c903a55fe
langcode: en
status: true
dependencies:
  config:
    - field.storage.paragraph.field_sysctl
    - paragraphs.paragraphs_type.app
id: paragraph.app.field_sysctl
field_name: field_sysctl
entity_type: paragraph
bundle: app
label: "SysCtl\t"
description: "sysctls defines kernel parameters to set in the container. sysctls can use either an array or a map.<br>\r\nYou can only use sysctls that are namespaced in the kernel. Docker does not support changing sysctls inside a container that also modify the host system. For an overview of supported sysctls, refer to https://docs.docker.com/reference/cli/docker/container/run/#sysctl."
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings: {  }
field_type: string_long
